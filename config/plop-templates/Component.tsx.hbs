/**
 * @author {{ userName }}
 * @since {{ created }}
 */

import * as React from "react";
import styled from "styled-components";
import { ITheme } from "@ui/theme";

interface Props {
  children?: React.ReactNode;
  theme: ITheme;
}

const {{pascalCase name}} = styled.div``;

export {
  {{pascalCase name}},
}